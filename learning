// we can run typescript file run time without installing any transpile compile use tsc it will convert it into js
// we can run typescript file run time without installing any transpile compile use tsc it will
// jest config 
